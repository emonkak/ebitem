{
  "name": "counter",
  "version": "0.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@emonkak/tempura": "*"
  }
}
